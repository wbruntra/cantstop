# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|----------------|-------------------|

## User Preferences
- Keep dependencies up to date when asked.

## Patterns That Work
- Use `npm view <pkg> version` to check latest versions before updating `package.json`.
- Run `npm install` after updating versions and verify with `npm run build`/`npm run lint`.

## Patterns That Don't Work

## Domain Notes
- React + Vite project using gh-pages for deployment.
